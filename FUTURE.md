BUG:
import export also template download on entities need testing and fixing

UI improve:
In preview the login info admin admin is not clear enough
during preview and in general generated erp needs to have a return button when in preview users leave preview while attempting to return to a previous screen

general improve:
during preview generation all other pages remain loading until preview is resolved prevent that bug

From chatbot chat answer questions.
Easy deployment service for users.
Automatically add requested feature.
Export whole erp Import whole erp.
