BUG:
in generated erp import export also template download on entities need testing and fixing
after a project is generated and entered to preview sometimes not always, could not find what triggers it, but sometimes after users backs out to another page and comes back preview is not shown it is back to previous step erp generation even though in projects it appears ready
during preview generation all other pages remain loading until preview is resolved prevent that bug


UI improve:
In preview the login info admin admin is not clear enough for users that are not really tech literate they try to enter their platform username password so we should make it more clear that they need to enter with admin admin initially and can change it I do not how though
during preview and in general generated erp needs to have a return button that goes back to previous pages etc when in preview users leave preview while attempting to return to a previous screen


Future features:
From chatbot chat answer questions.
Easy deployment service for users.
Automatically add requested feature.
Export whole erp Import whole erp.
