# {{PROJECT_NAME}}

> Generated by [CustomERP](https://github.com/CustomERP411/CustomERP) - AI-Powered ERP Generation Platform

---

## Overview

This ERP system was automatically generated based on your business requirements.

**Generated at:** {{GENERATED_AT}}  
**Module:** {{MODULE_TYPE}}

---

## Quick Start

### Using Docker (Recommended)

```bash
# Start the application
docker compose up -d

# View logs
docker compose logs -f

# Stop the application
docker compose down
```

### Without Docker

```bash
# Install dependencies
npm install

# Start the server
npm start

# Or run in development mode with auto-reload
npm run dev
```

---

## Access Points

| Service | URL | Description |
|:--------|:----|:------------|
| **Backend API** | http://localhost:{{BACKEND_PORT}} | REST API |
| **Frontend** | http://localhost:{{FRONTEND_PORT}} | Web Interface |

---

## API Endpoints

{{#each ENTITIES}}
### {{display_name}} (`/api/{{slug}}`)

| Method | Endpoint | Description |
|:-------|:---------|:------------|
| `GET` | `/api/{{slug}}` | List all {{display_name}} |
| `GET` | `/api/{{slug}}/:id` | Get single {{display_name}} |
| `POST` | `/api/{{slug}}` | Create new {{display_name}} |
| `PUT` | `/api/{{slug}}/:id` | Update {{display_name}} |
| `DELETE` | `/api/{{slug}}/:id` | Delete {{display_name}} |

**Fields:**
{{#each fields}}
- `{{name}}` ({{type}}){{#if required}} - Required{{/if}}
{{/each}}

{{/each}}

---

## Data Storage

This application uses **JSON flat-files** for data storage.

Data files are located in the `/data` directory:
{{#each ENTITIES}}
- `data/{{slug}}.json` - {{display_name}} records
{{/each}}

---

## Project Structure

```
{{PROJECT_SLUG}}/
├── docker-compose.yml      # Container orchestration
├── Dockerfile              # Backend container
├── package.json            # Dependencies
├── README.md               # This file
├── src/
│   ├── index.js            # Application entry point
│   ├── routes/             # API route handlers
│   ├── services/           # Business logic
│   └── repository/         # Data access layer
├── frontend/               # React frontend
│   ├── src/
│   └── Dockerfile
└── data/                   # JSON data files
    {{#each ENTITIES}}
    ├── {{slug}}.json
    {{/each}}
```

---

## Environment Variables

Create a `.env` file if you need to customize:

```env
PORT={{BACKEND_PORT}}
NODE_ENV=production
```

---

## Features

{{#if FEATURES.stock_tracking}}
- ✅ **Stock Tracking** - Monitor inventory quantities
{{/if}}
{{#if FEATURES.low_stock_alerts}}
- ✅ **Low Stock Alerts** - Notifications when stock falls below threshold
{{/if}}
{{#if FEATURES.audit_trail}}
- ✅ **Audit Trail** - Log all data changes
{{/if}}

---

## Support

This ERP was generated by CustomERP. For issues or modifications, regenerate from the CustomERP platform or manually edit the source files.

---

<p align="center">
  <em>Generated by CustomERP — Bridging Business Intent and Enterprise Software</em>
</p>

