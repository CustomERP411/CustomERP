# Overview Update Notes

Only TE (doc integrator) updates `docs/overview.md` and `docs/prompt_expectations.md`.

Everyone else adds update notes here:

`docs/overview_updates/<initials>_<YYYYMMDD>_<topic>.md`

These notes are merged by TE into the main docs.
