# ================================
# CustomERP Frontend - Development
# ================================
FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

# Source will be mounted as volume
ENV NODE_ENV=development

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]

